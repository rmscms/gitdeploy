<Page x:Class="GitDeployPro.Pages.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      Title="HistoryPage"
      Background="{StaticResource Surface.Base}">
    <Grid Margin="{StaticResource Spacing.PageMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="📜" FontSize="40" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="{StaticResource Text.Primary}"/>
                <StackPanel>
                    <TextBlock Text="Deployment History" Style="{StaticResource App.PageTitleTextStyle}" />
                    <TextBlock Text="Track deploy status, details and rollback checkpoints."
                               Style="{StaticResource App.PageSubtitleTextStyle}" />
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1"
                   Height="42"
                   Padding="20,0"
                   Style="{StaticResource App.Button.Primary}"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Cursor="Hand"
                   Click="Refresh_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔄" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource Text.Primary}"/>
                    <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- History List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="HistoryItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource Surface.Card}" 
                               BorderBrush="{StaticResource Border.Subtle}"
                               BorderThickness="1"
                               CornerRadius="10" 
                               Padding="20" 
                               Margin="0,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon Circle -->
                                <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                                       Background="{Binding StatusColor}" 
                                       CornerRadius="30" 
                                       Width="50" 
                                       Height="50"
                                       Margin="0,0,15,0"
                                       VerticalAlignment="Top">
                                    <TextBlock Text="{Binding Icon}" 
                                              FontSize="26" 
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Foreground="{StaticResource Text.Inverse}"/>
                                </Border>

                                <!-- Title -->
                                <TextBlock Grid.Row="0" Grid.Column="1"
                                          Text="{Binding Title}" 
                                          FontSize="17" 
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource Text.Primary}"/>

                                <!-- Status Badge -->
                                <Border Grid.Row="0" Grid.Column="2" 
                                       Background="{Binding StatusColor}" 
                                       CornerRadius="4" 
                                       Padding="12,6"
                                       VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" 
                                              Foreground="{StaticResource Text.Inverse}" 
                                              FontSize="12" 
                                              FontWeight="Bold"/>
                                </Border>

                                <!-- Details -->
                                <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                          Text="{Binding Details}" 
                                          FontSize="13" 
                                          Foreground="{StaticResource Text.Muted}" 
                                          Margin="0,6,0,12"/>

                                <!-- Actions -->
                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
                                           Orientation="Horizontal" 
                                           Margin="0,8,0,0">
                                    <Button Height="36" 
                                           Padding="16,0"
                                           Margin="0,0,10,0"
                                           Style="{StaticResource App.Button.Ghost}"
                                           Foreground="{StaticResource Text.Secondary}"
                                           FontSize="12"
                                           Cursor="Hand"
                                           Click="Details_Click"
                                           Tag="{Binding Id}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📋" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{StaticResource Text.Primary}"/>
                                            <TextBlock Text="Details" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Height="36" 
                                           Padding="16,0"
                                           Style="{StaticResource App.Button.Ghost}"
                                           Foreground="{StaticResource Text.Secondary}"
                                           FontSize="12"
                                           Cursor="Hand"
                                           Click="Rollback_Click"
                                           Tag="{Binding Id}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="↩️" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="{StaticResource Text.Primary}"/>
                                            <TextBlock Text="Rollback" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
