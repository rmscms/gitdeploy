<Window x:Class="GitDeployPro.Windows.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="180" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="#202020" CornerRadius="8" BorderBrush="#3F3F3F" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Text="Input" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>

            <StackPanel Grid.Row="1">
                <TextBlock x:Name="MessageText" Text="Please enter value:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                <TextBox x:Name="InputTextBox" Background="#3A3A3A" Foreground="White" Padding="5" Height="30"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="Cancel" Width="80" Height="30" Background="Transparent" Foreground="#CCCCCC" BorderBrush="#555555" BorderThickness="1" Margin="0,0,10,0" Click="Cancel_Click"/>
                <Button Content="OK" Width="80" Height="30" Background="#0078D4" Foreground="White" BorderThickness="0" Click="OK_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
