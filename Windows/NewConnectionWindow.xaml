<Window x:Class="GitDeployPro.Windows.NewConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Database Connection" 
        Height="420" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Surface.Base}"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Border Background="{StaticResource Surface.Raised}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="1" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource Surface.Card}" CornerRadius="10,10,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                        <TextBlock Text="➕" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="New Database Connection" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource Text.Primary}"/>
                    </StackPanel>
                    <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" 
                            Width="30" Height="30" Style="{StaticResource App.IconButton.Close}"
                            Click="CloseButton_Click" Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- Form -->
            <StackPanel Grid.Row="1" Margin="24,20">
                <TextBlock Text="Database Type" Foreground="{StaticResource Text.Muted}" FontSize="12" Margin="0,0,0,6"/>
                <ComboBox x:Name="DbTypeCombo" Height="34" Margin="0,0,0,14">
                    <ComboBoxItem Content="MySQL" Tag="MySQL" IsSelected="True"/>
                    <ComboBoxItem Content="MariaDB" Tag="MariaDB"/>
                </ComboBox>

                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Host" Foreground="{StaticResource Text.Muted}" FontSize="12" Margin="0,0,0,6"/>
                        <TextBox x:Name="HostBox" Height="34" Text="127.0.0.1"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Port" Foreground="{StaticResource Text.Muted}" FontSize="12" Margin="0,0,0,6"/>
                        <TextBox x:Name="PortBox" Height="34" Text="3306" HorizontalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Username" Foreground="{StaticResource Text.Muted}" FontSize="12" Margin="0,0,0,6"/>
                <TextBox x:Name="UsernameBox" Height="34" Text="root" Margin="0,0,0,14"/>

                <TextBlock Text="Password" Foreground="{StaticResource Text.Muted}" FontSize="12" Margin="0,0,0,6"/>
                <PasswordBox x:Name="PasswordBox" Height="34" Margin="0,0,0,14"/>

                <TextBlock Text="Database (optional)" Foreground="{StaticResource Text.Muted}" FontSize="12" Margin="0,0,0,6"/>
                <TextBox x:Name="DatabaseBox" Height="34"/>
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="2" Background="{StaticResource Surface.Card}" CornerRadius="0,0,10,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,16,0">
                    <Button Content="Cancel" Width="90" Height="34" Style="{StaticResource App.Button.Ghost}" Foreground="{StaticResource Text.Secondary}" Click="CancelButton_Click" Cursor="Hand" Margin="0,0,10,0"/>
                    <Button x:Name="ConnectButton" Content="Connect" Width="100" Height="34" Style="{StaticResource App.Button.Primary}" Foreground="{StaticResource Text.Primary}" Click="ConnectButton_Click" Cursor="Hand" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>

