<mah:MetroWindow x:Class="GitDeployPro.Windows.BackupProgressWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="Backup progress"
                 Width="520"
                 Height="340"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ShowCloseButton="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ModeLabel"
                   Text="Backup progress"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="#8BD7FF"/>
        <TextBlock Grid.Row="1"
                   Text="Overall progress"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="White"/>
        <ProgressBar x:Name="OverallProgressBar"
                     Grid.Row="2"
                     Height="14"
                     Margin="0,8,0,0"
                     Minimum="0"
                     Maximum="1"
                     Value="0"/>
        <TextBlock x:Name="OverallSummaryText"
                   Grid.Row="3"
                   Margin="0,6,0,0"
                   Foreground="#B0FFCF"
                   FontSize="13"
                   Text="Waiting for backup …"/>

        <Border Grid.Row="4"
                Background="#242424"
                CornerRadius="10"
                Padding="16"
                Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="Current table"
                           FontWeight="SemiBold"
                           Foreground="White"/>
                <TextBlock x:Name="CurrentTableName"
                           Margin="0,4,0,0"
                           Foreground="#E0E0E0"
                           FontSize="13"
                           Text="(none)"/>
                <ProgressBar x:Name="TableProgressBar"
                             Height="12"
                             Margin="0,10,0,0"
                             Minimum="0"
                             Maximum="1"
                             Value="0"/>
                <TextBlock x:Name="TableSummaryText"
                           Margin="0,6,0,0"
                           Foreground="#CCCCCC"
                           FontSize="12"
                           Text="Awaiting table …"/>
                <ScrollViewer Margin="0,12,0,0"
                              Height="140"
                              VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="LogTextBlock"
                               TextWrapping="Wrap"
                               Foreground="#9DD6FF"/>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</mah:MetroWindow>

