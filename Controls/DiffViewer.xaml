<UserControl x:Class="GitDeployPro.Controls.DiffViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
             x:Name="Root">
    <Border Background="#1A1A1A" CornerRadius="8" Padding="15" BorderBrush="#2A2A2A" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,5">
                <TextBlock Text="{Binding Title, ElementName=Root}" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Foreground="White"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Status, ElementName=Root}"
                           FontSize="12"
                           Foreground="#999999"/>
            </StackPanel>

            <Border Grid.Row="1" Background="#2F2F2F" Height="1" Margin="0,5"/>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <avalon:TextEditor x:Name="DiffEditor"
                                   FontFamily="Consolas"
                                   FontSize="12"
                                   Background="#101010"
                                   Foreground="#E0E0E0"
                                   ShowLineNumbers="True"
                                   IsReadOnly="True"
                                   HorizontalScrollBarVisibility="Auto"
                                   VerticalScrollBarVisibility="Auto"
                                   Padding="4"
                                   BorderThickness="0"/>

                <StackPanel x:Name="EmptyState"
                            Visibility="Collapsed"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <TextBlock Text="{Binding EmptyMessage, ElementName=Root}"
                               FontSize="13"
                               Foreground="#888888"
                               TextAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>

