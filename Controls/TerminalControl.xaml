<UserControl x:Class="GitDeployPro.Controls.TerminalControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource Surface.Base}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- Terminal Output -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource Surface.Card}" Padding="10,5" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Connection Info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="SSH Terminal" Foreground="{StaticResource Text.Primary}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Border x:Name="StatusIndicator" Width="10" Height="10" CornerRadius="5" Background="{StaticResource Text.Muted}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock x:Name="StatusText" Text="Disconnected" Foreground="{StaticResource Text.Muted}" VerticalAlignment="Center" FontSize="12"/>
                </StackPanel>

                <!-- Settings / Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="ConnectButton" Content="🔌 Connect" Click="ConnectButton_Click" 
                            Style="{StaticResource App.Button.Primary}" Foreground="{StaticResource Text.Primary}" Padding="10,4" Margin="0,0,10,0" Cursor="Hand"/>
                    
                    <Button x:Name="DetachButton" Content="🗖 Pop-out" Click="DetachButton_Click"
                            Style="{StaticResource DetachPageButtonStyle}" Margin="0,0,10,0"/>

                    <Button x:Name="ClearButton" Content="🧹 Clear" Click="ClearButton_Click"
                            Style="{StaticResource App.Button.Ghost}" Foreground="{StaticResource Text.Secondary}" Padding="8,4" Margin="0,0,10,0" Cursor="Hand"/>

                    <ToggleButton x:Name="TypeToggleButton" Content="⌶" ToolTip="Focus/Type (Enable/Disable typing)"
                                  Width="32" Height="28" Margin="0,0,10,0"
                                  Background="{StaticResource Surface.Input}" Foreground="{StaticResource Text.Secondary}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="1" Cursor="Hand"
                                  Checked="TypeToggleButton_Checked" Unchecked="TypeToggleButton_Unchecked"
                                  IsChecked="True">
                        <ToggleButton.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="3"/>
                            </Style>
                        </ToggleButton.Resources>
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton">
                                <Setter Property="Background" Value="{StaticResource Surface.Input}"/>
                                <Setter Property="Foreground" Value="{StaticResource Text.Secondary}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource Border.Subtle}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="Bg"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource State.Hover}"/>
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource Surface.Raised}"/>
                                                    <Setter TargetName="Bg" Property="BorderBrush" Value="{StaticResource Accent.Primary}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource Text.Primary}"/>
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="False">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource Surface.Input}"/>
                                                    <Setter TargetName="Bg" Property="BorderBrush" Value="{StaticResource Border.Subtle}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource Text.Secondary}"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="Bg" Property="Opacity" Value="0.6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter Property="Background" Value="{StaticResource Surface.Raised}"/>
                                        <Setter Property="Foreground" Value="{StaticResource Text.Primary}"/>
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="False">
                                        <Setter Property="Background" Value="{StaticResource Surface.Input}"/>
                                        <Setter Property="Foreground" Value="{StaticResource Text.Secondary}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource State.Hover}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ToggleButton.Style>
                    </ToggleButton>

                    <ComboBox x:Name="FontSizeCombo" Width="50" SelectedIndex="2" SelectionChanged="FontSizeCombo_SelectionChanged" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="10"/>
                        <ComboBoxItem Content="12"/>
                        <ComboBoxItem Content="14"/>
                        <ComboBoxItem Content="16"/>
                        <ComboBoxItem Content="18"/>
                        <ComboBoxItem Content="20"/>
                    </ComboBox>

                    <ComboBox x:Name="TextColorCombo" Width="80" SelectedIndex="0" Margin="10,0,0,0" SelectionChanged="TextColorCombo_SelectionChanged" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Gray" Tag="#D4D4D4"/>
                        <ComboBoxItem Content="White" Tag="#FFFFFF"/>
                        <ComboBoxItem Content="Green" Tag="#00FF00"/>
                        <ComboBoxItem Content="Cyan" Tag="#00FFFF"/>
                        <ComboBoxItem Content="Yellow" Tag="#FFFF00"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Terminal Screen -->
        <Grid Grid.Row="1" Background="{StaticResource Surface.Base}">
            <wv:WebView2 x:Name="TerminalWebView"/>

            <Border x:Name="TypingOverlay"
                    Background="#66000000"
                    Visibility="Collapsed"
                    IsHitTestVisible="False">
                <TextBlock Text="Typing disabled"
                           Foreground="#E0E0E0"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
